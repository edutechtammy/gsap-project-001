// js/animations/joe/joeIdle.js

export function buildJoeIdle() {
    // Return an empty timeline so callers don't crash
    // You'll add the idle animation here later
    return gsap.timeline({ repeat: -1 });
}