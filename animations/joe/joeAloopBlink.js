// js/animations/joe/joeAloopBlink.js

export function buildJoeBlink() {
    // Return an empty timeline so callers don't crash
    // You'll add the blink animation here later
    return gsap.timeline({ repeat: -1 });
}